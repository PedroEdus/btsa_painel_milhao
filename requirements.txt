streamlit>=1.42          # auth nativa OIDC (st.login/st.user)
Authlib>=1.3.2           # backend OIDC exigido pela auth do Streamlit
httpx>=0.27              # cliente HTTP usado pelo Authlib no fluxo OIDC
pandas>=2.2
numpy>=1.26
plotly>=5.24
python-dotenv>=1.0
tzdata>=2024.1           # base zoneinfo (America/Sao_Paulo) no Windows
pyarrow>=17.0
# Conexão OneLake (Parquet snapshot via Azure Data Lake Storage Gen2)
azure-identity>=1.17
azure-storage-file-datalake>=12.0
# Testes
pytest>=8.0
